# LoCoMo Q20 Dataset Configuration
# First 20 questions from conv0 with required sessions

name: "locomo-q20"
version: "1.0"
description: "First 20 questions from LoCoMo conv0 (9 sessions, 191 messages)"

# Data configuration
data:
  path: "locomo/locomo-q20.json"  # Relative to eval/data/ or project root data/
  format: "locomo"  # Native format, no conversion needed

# Evaluation configuration
evaluation:
  type: "llm_judge"  # llm_judge | exact_match

  # LLM Judge settings
  llm:
    provider: "openai"
    model: "gpt-4o-mini"
    api_key: "${OPENAI_API_KEY}"
    base_url: "https://api.openai.com/v1"

  num_runs: 3  # Number of judging runs per question

  # Filter settings
  filter_category: [5]  # Filter out category 5 (adversarial questions)
